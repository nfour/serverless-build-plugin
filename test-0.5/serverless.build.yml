method: "bundle"

sourceMaps : true
babel      : true
uglify     : true

excludes:
  - "*" # Ignores the root directory

functions:
  test:
    include:
      - "test/**"
      - "lib/one/**"

    exclude:
      - "**/*.json"

  blah:
    include:
      - "blah/**"

    exclude:
      - "**/*.json"
