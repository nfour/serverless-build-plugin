method: "bundle"

sourceMaps : true
babel      : true
uglify     : true

exclude:
  - "*" # Ignores the root directory

functions:
  one:
    include:
      - "functions/one/**"
      - "lib/one/**"

    exclude:
      - "**/*.json"

  two:
    exclude:
      - "lib/one/**"
