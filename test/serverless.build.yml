method: "bundle"

sourceMaps : true
babel      : true
uglify     : true

excludes:
  - "*" # Ignores the root directory

functions:
  one:
    includes:
      - "functions/one/**"
      - "lib/one/**"

    excludes:
      - "**/*.json"

  two:
    excludes:
      - "lib/one/**"
