exclude:
  - * # Root dir files can fuck off

functions:
  one:
    include:
      - functions/one/**
      - lib/one/**
      - !!js/regexp .+(js|json)$

    exclude:
      - lib/one/*.json

  two:
    exclude:
      - lib/one/**
