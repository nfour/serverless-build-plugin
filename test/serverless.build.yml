method: "bundle"

babel  : true
uglify : null

excludes:
  - "*" # Ignores the root directory

functions:
  one:
    includes:
      - "functions/one/**"
      - "lib/one/**"

    excludes:
      - "**/*.json"

  two:
    excludes:
      - "lib/one/**"
