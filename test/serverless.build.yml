excludes:
  - ./*
functions:
  one:
    includes:
      - functions/one/**
      - lib/one/**
      - !!js/regexp .+(js|json)$

    excludes:
      - lib/one/*.json

  two:
    excludes:
      - lib/one/**
